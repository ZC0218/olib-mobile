name: iOS Enterprise Build

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-14

    steps:
      # 1️⃣ 拉取代码
      - uses: actions/checkout@v4

      # 2️⃣ 安装 Flutter
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.41.5"

      # 3️⃣ 安装依赖
      - run: flutter pub get

      # 4️⃣ 修复 Flutter iOS 工程 org 冲突
      - run: flutter create . --org com.openlibrary

      # 5️⃣ 导入企业证书
      - name: Install Certificate
        env:
          P12_BASE64: ${{ secrets.P12_BASE64 }}
          P12_PASSWORD: ${{ secrets.P12_PASSWORD }}
        run: |
          echo "$P12_BASE64" | base64 --decode > cert.p12
          security create-keychain -p "" build.keychain
          security default-keychain -s build.keychain
          security unlock-keychain -p "" build.keychain
          security import cert.p12 -k build.keychain -P "$P12_PASSWORD" -T /usr/bin/codesign
          security set-key-partition-list -S apple-tool:,apple: -s -k "" build.keychain

      # 6️⃣ 安装描述文件
      - name: Install Provisioning Profile
        env:
          PROVISION_BASE64: ${{ secrets.PROVISION_BASE64 }}
        run: |
          echo "$PROVISION_BASE64" | base64 --decode > profile.mobileprovision
          mkdir -p ~/Library/MobileDevice/Provisioning\ Profiles
          cp profile.mobileprovision ~/Library/MobileDevice/Provisioning\ Profiles/

      # 7️⃣ 修复 Podfile 指定最低 iOS 版本，并安装 Pods
      - name: Install Pods
        run: |
          cd ios
          # 指定 iOS 12.0 作为最低版本
          sed -i '' "s/platform :ios, .*/platform :ios, '12.0'/" Podfile || echo "platform :ios, '12.0'" >> Podfile
          pod install --repo-update

      # 8️⃣ 调试 ios 文件夹，确认 ExportOptions.plist 存在
      - name: Debug ios directory
        run: |
          pwd
          ls -R ios

      # 9️⃣ 构建 IPA
      - name: Build IPA
        run: flutter build ipa --release --export-options-plist=ios/ExportOptions.plist

      # 🔟 上传 IPA
      - uses: actions/upload-artifact@v4
        with:
          name: ipa
          path: build/ios/ipa/*.ipa
